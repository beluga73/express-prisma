PORT=3000
CLIENT_URL=http://localhost:5173

DB_USER=postgres
DB_PASSWORD=password123
DB_NAME=dev_db
DB_PORT=5432

DATABASE_URL="postgresql://postgres:password123@localhost:5432/dev_db"

# Secrets used to sign access/refresh tokens and the refresh-token cookie.
# Generate a random value for each with: openssl rand -hex 32
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
COOKIE_SECRET=
