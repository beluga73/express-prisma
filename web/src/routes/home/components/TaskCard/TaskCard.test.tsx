/**
 * Should we even write RTL test for TaskCard?
 * Before I wanted becasue it was one component that was not using BoardStore
 * But it uses it as well, do we even need to write those tests?
 * We can easily mock that boardStore but honestly do we need that
 */
